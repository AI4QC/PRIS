# NEXT44 rich analytic feature implementation plan

1. Add tests for deterministic single-x0 rich descriptors, explicit fail-open families, strict NEXT42 geometry-only input validation, hashing, and no-replace publication.
2. Implement `src/next44_rich_analytic_features.py` by calling frozen analytic kernels and adding small direct geometry/composition summaries.
3. Build and seal all 2,285 rows outside the repository, without reading NEXT42 evaluation outputs.
4. Add tests for strict NEXT43+NEXT44 table joining and unchanged discovery-only formula selection.
5. Implement `src/next44_rich_law_search.py`, reuse the exact NEXT43 finite catalogue/gates, and compare the selected formula against NEXT43, NEXT23, and Pauling.
6. If and only if both internal splits pass, create a separate frozen confirmation package before acquiring new endpoints. Otherwise keep confirmation closed and report the best calibrated improvement as development evidence only.
7. Run full tests and create a standalone report without editing paper, README, or older content.
