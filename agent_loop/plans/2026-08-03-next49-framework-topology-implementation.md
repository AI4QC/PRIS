# NEXT49 framework topology implementation plan

1. Freeze the official ODAC23 artifact URL, byte count, published MD5, and local
   SHA-256 without listing or extracting the archive.
2. Add invariant tests for periodic graph translation rank, discrete versus
   framework classification, fail-open behavior, and local metal-vector
   distortion.
3. Implement the geometry-only feature kernel and sealed QMOF batch builder in
   a new module.  Do not edit NEXT27, NEXT31, or NEXT48.
4. Run the focused tests, build the 4,119-row feature artifact from the already
   sealed NEXT48 x0 archive, and verify manifests and support accounting.
5. Join labels only after the feature artifact is sealed.  Search a deliberately
   small standardized linear formula using source-family leave-one-out
   development diagnostics, freeze the selected terms and threshold, then rerun
   from frozen artifacts.
6. Freeze the ODAC evaluation protocol before opening the archive.  Exclude
   QMOF identity overlap and adsorbates analytically, evaluate once, and write a
   standalone report only after the result is known.
7. Run focused and full repository tests, syntax checks, artifact hash checks,
   and CodeGraph status.  Preserve every previous script, result, and report.
