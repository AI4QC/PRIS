# NEXT74 one-shot internal validation of frozen NEXT73

## Frozen candidate

Evaluate only formula SHA-256
`a2b1033ee1b8ad99254ed005215322306ce8e09897bd2426454df83c4c9143c8`.
Its five features, centers, scales, signs, weights, domain/missing policy, and
threshold 4.268946011516139 are immutable.  No fitting, threshold calibration,
feature selection, imputation, or alternative candidate is allowed.

## One-shot evaluation

Use the already label-free NEXT70 feature artifact and only the physically
separate NEXT60 `internal_validation` robust endpoint file.  Join by exact
material ID, apply the frozen formula once, and compute the unchanged seven
gates and four defective/open-metal-site strata.  Publish the complete metrics
and row predictions atomically.  If any gate fails, stop without opening
internal replication.  If all pass, seal the validation artifact before a
separate one-shot replication protocol is written.

## Boundary

The executable formula consumes only raw x0 analytic features.  Validation
labels are offline outcomes and never become formula inputs.  No DFT
calculation, relaxed geometry, energy/force/stress model, proxy potential,
physical relaxation, or same-composition alternative is executed or read by
the formula.
