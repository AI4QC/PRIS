# NEXT391--NEXT394 periodic skeletal ball-growth freeze

## Frozen boundary and missing mechanism

This additive branch is frozen before any NEXT391 value or outcome is opened.
The executable accepts only composition and one finite raw unrelaxed periodic
geometry, and forbids every DFT calculation/value, energy/force/stress proxy,
model potential, relaxation, trajectory, validation and replication input.

Coordination sequences count distinct periodic-net vertices at graph distance
`n` and have quasi-polynomial growth in crystals.  The repository measures
rank, rigidity, closures, three-step path collisions and local vertex bypass,
but not the number of distinct atom images reached within a fixed graph ball.

## Graph and sole formula

Use exactly the frozen NEXT379/NEXT383 firewall, reduced cell, ordinary
periodic Voronoi facets, reverse checks, mutual solid-angle salience,
quantization, exact-tie filtration and global PSNB q10 rank-three threshold
`tau`.  Retain every edge with salience `>=tau`.

For each site `i`, breadth-first search the infinite lifted skeleton and count
`B_i(4)`, the number of distinct lifted vertices at shortest graph distance at
most four, including `(i,0)`.  Depth four is frozen as the first compact ball
covering the conventional local three- through six-member ring range while
sampling beyond the immediate environment.  Define

    g_i = (B_i(4)/4^3) / (1 + B_i(4)/4^3)

and the sole feature

    psbg_skeletal_ball4_growth_q10 = inverted-CDF q10_i g_i.

The transform only maps the nonnegative growth coefficient into `[0,1)`.
The sole direction is `protected_high`.  No alternate radius, normalization,
summary, direction, transform, threshold, weight or conjunction is allowed.

## Pre-outcome loop

Analytic tests require exact chain and simple-cubic ball counts (`9` and
`129`), ties, order/scale/disjoint replication, malformed facets, distorted
NaCl, rigid and periodic representation equivalences, supercell invariance and
the no-DFT firewall.

The unchanged 80+80 label-blind probe must satisfy per source: support at least
72, finite `[0,1]`, at least 20 rounded values, representation error at most
`1e-8`, and maximum absolute Spearman strictly below `0.90` against every
adequate prior formal label-free control through NEXT383.  It must additionally
recompute the rejected NEXT387 PSVB value on the same rows and include it in
the novelty comparison.  No endpoint is opened.

Only a fully passing probe authorizes a complete discovery build with coverage
at least `0.95`.  Only a valid build authorizes one frozen
`psbg_skeletal_ball4_growth_q10__protected_high` audit in the exact NEXT224
rejected-extreme cohort and unchanged `(1/16,15/16)` bounded map.  Per-source
gates remain pooled/macro AUC `>=0.55`, worst-fold AUC `>=0.50`, coverage
`>=0.95`, and pooled/every-fold class counts `>=20`; both sources must pass.
Failure creates no later search.  Passing authorizes only a separately frozen
search and does not establish validation or replacement of existing laws.

Canonical papers, notes, README and preregistration remain untouched; results
go only to the independent additive report pending user review.
