# NEXT28 implementation plan

1. Add tests and an additive fixed-law freeze/application module.
2. Freeze `C_1.05 >= 6.3` on the six newly opened NEXT27 shards without a
   threshold search.
3. Opaque-byte extract later OMC25 archive members; exclude every CSD refcode
   used by NEXT26/NEXT27 and earlier selected NEXT28 shards.
4. Sanitize x0, build the unchanged NEXT27 periodic-contact features, and
   checksum-freeze NEXT28 predictions before endpoint access.
5. Assemble exact-identity endpoints, evaluate once against frozen gates, and
   retain the result whether it passes or fails.
6. Run focused/full tests and write a new standalone report.  Do not edit any
   existing report, canonical paper, README, or prior script.
