# NEXT13c ACSC-DIRECT-LADDER-v0 Plan

The first direct confirmation used `h=2^-8` and `h/2=2^-9`. MatterSim total
energies visibly quantize the smaller curvature, so this additive falsification
does not loosen the sign rule. It independently evaluates `h=2^-7` and
`h/2=2^-8` for the same 123 sealed modes.

A nested confirmation requires both frozen direct pairs to be resolved negative:

```text
small pair: 2^-8 and 2^-9 -> resolved_negative
large pair: 2^-7 and 2^-8 -> resolved_negative
```

Thus the retained mode is negative across three distinct amplitudes. The
`2^-8` midpoint is independently repeated in a different predictor call and its
curvature agreement is recorded. No DFT endpoint, label, threshold fitting, or
existing report modification is allowed.
