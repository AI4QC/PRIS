# Figure regeneration from the committed aggregate data needs only these two.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Structure-level analysis (src/validity_rulesets.py, src/rank_rulesets.py,
# src/apply_rules.py) additionally needs pymatgen and an external feature store.
pymatgen>=2024.1.1
pyarrow>=14.0

# Certified-optimal decision trees (Section "Search was never the binding constraint")
pystreed==1.3.9

# Symbolic regression for the two continuous scores
pysr>=0.18
ase
scipy
scikit-learn
spglib
